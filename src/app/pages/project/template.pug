v-container#project(grid-list-md)
    h3 Project: {{project.name}}

    hr
    h3 Scenes
    v-layout.scene-list(row wrap)
        v-flex(xs6 sm4 md3 lg2 v-for="item, i in project.scenes" :key="item.id")
            v-card.project-card(@click.native="selectScene(item)")
                v-card-media(src=require('assets/images/scene.png') height="200")
                v-card-title {{item.name}}
