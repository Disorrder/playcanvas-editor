$root = '../../../'
@require $root + 'app/mixins.styl'

#projects
    .project-list
        .card
            elevationTransition()
            
            &:hover:not([disbled])
                cursor pointer
                elevation(6, true)
